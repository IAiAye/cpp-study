is a 관계도 있는가 하면 has a 관계 또한 존재한다.

자동차 클래스가 있다 할때, 엔진 클래스, 브레이크 클래스 등이 있을 것이다.
이때 이러한 클래스들은 is a 관계라고 할 수 없다.
이러한 관계는 has a 관계라고 할 수 있다.

자동차 has a 엔진

class Car
{
    private:
    Engine e;
    Brake b;
    ...
};
와 같이 표현하게 되면 has a 라고 할 수 있다.